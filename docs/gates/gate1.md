# Gate 1 conformance report

*Generated by `tools/gate_report.py`. Regenerate after any settings or register change. This file is written, never edited.*

## Counts

Settings against the node register. Every row is compared; none is assumed.

| What | Settings | Register | |
|---|---:|---:|---|
| nodes | 79 | 79 | OK |
| emitting rows | 49 | 49 | OK |
| columns | 56 | 56 | OK |
| law T1 | 18 | 18 | OK |
| law T2 | 9 | 9 | OK |
| law T3 | 17 | 17 | OK |
| law T4 | 35 | 35 | OK |

## Startup checks

Each check ran against the loaded settings. A failure raises and no report is written, so a report that exists is a report whose checks passed.

| Check | What it holds | Result |
|---|---|---|
| V1 | every parent exists and the graph is acyclic | PASS |
| V2 | one entry per node, no duplicates | PASS |
| V4 | the emitting rows are exactly the frozen column set | PASS |
| V5 | every parent is read by its child's law | PASS |
| V6 | every probability table's rows sum to 1 | PASS |
| V7 | every calibration path resolves | PASS |
| V8 | every node's rng stream is declared | PASS |
| V9 | every declared stream is named by some node | PASS |
| V10 | every emitting node states its 4-view observability | PASS |
| checksums | the 6 calibration CSVs match their recorded hashes | PASS |
| V3 | every deterministic law names a function that exists | **deferred to stage 2**, it needs the generator |
| V11 | no key is set twice at the same layer | PASS, during the merge |

## Auxiliaries reached by lookup

These emit no column and no law names them as a parent. The specification's scope rule says a pool quantity enters auction scope through a lookup, so this is expected. Listed because a genuinely dead node looks identical.

`app_i`, `c_idx`, `sample_weight`, `u_rows`

## V5, parents not confirmed in the law text

The register writes laws in prose, so a parent can be real and still not appear by name. These re-check in stage 2 when the laws bind to functions.

| Node | Parents not found in its law text |
|---|---|
| `A2` | `A1` |
| `A4` | `A3` |
| `c_idx` | `user_vbin` |
| `_size` | `B6` |
| `D2` | `LU1` |
| `LU7` | `b_k`, `participate_k` |
| `H9` | `participate_k` |
| `H4` | `H3`, `sold_lost`, `H2`, `LU7` |

## Specification gaps

Summary only. The full list, with the hand-written entries, is `docs/SPEC_GAPS.md`.

| Kind | Count |
|---|---:|
| holes, no document supplies the value | 0 |
| unsourced, a value with no route or source | 0 |
| undeclared, a register key with no home in the config | 6 |

## Gate 1 verdict

**Gate 1: PASS**

6 undeclared register keys reported, not gating.

